<%= form_with(model: review_report) do |form| %>
  <% if review_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review_report.errors.count, "error") %> prohibited this review_report from being saved:</h2>

      <ul>
        <% review_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
